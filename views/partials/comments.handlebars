<div class="comments">
  <h4>Comments</h4>
  {{#each this}}
    <section class="comment">
      <div class="meta">
        {{user.username}}:
      </div>
      <div class="text">
        {{comment_text}}
      </div>
    </section>
  {{/each}}
</div>